[project]
name = "toy-pickplace"
version = "0.1.0"
description = "MuJoCo tabletop pick-and-place scene with Franka Panda"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "mujoco>=3.1.3",
]

[tool.uv]
package = false
